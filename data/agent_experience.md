# Agent experience

Tasks completed using generated code (runCode) or a saved skill, logged at runtime so
future plans can see what already worked instead of re-deriving it from scratch. Stand-in
for the planned RAG/vector memory system, same role CraftingExperience.ts plays for recipes.

## Craft me and give me some doors
- Approach: runCode({"code":"const doorName = 'oak_door';\nconst plankName = 'oak_planks';\nconst logName = 'oak_log';\nconst tableName = 'crafting_table';\n\nasync function ensurePlanks(count) {\n  let planks = bot.inventory.count(plankName);\n  if (planks >= count) return;\n  \n  let logs = bot.inventory.count(logName);\n  if (logs === 0) {\n    const logBlock = bot.findBlock({ matching: mcData.blocksByName[logName].id, maxDistance: 32 });\n    if (!logBlock) throw new Error('No oak logs found nearby');\n    await bot.pathfinder.goto(new goals.GoalNear(logBlock.position.x, logBlock.position.y, logBlock.position.z, 1));\n    await bot.collectBlock.collect(logBlock);\n  }\n  \n  const recipe = bot.recipesFor(plankName, null, 1, null)[0];\n  if (!recipe) throw new Error('No recipe for planks');\n  await bot.craft(recipe, Math.ceil((count - planks) / 4));\n}\n\nasync function getCraftingTable() {\n  let table = bot.inventory.findFirstItem(tableName);\n  if (table) return table;\n  \n  await ensurePlanks(4);\n  const recipe = bot.recipesFor(tableName, null, 1, null)[0];\n  await bot.craft(recipe, 1);\n  return bot.inventory.findFirstItem(tableName);\n}\n\ntry {\n  await ensurePlanks(6);\n  const tableItem = await getCraftingTable();\n  await bot.equip(tableItem, 'hand');\n  \n  const tableBlock = bot.blockAt(bot.entity.position.offset(1, 0, 0));\n  await bot.placeBlock(tableBlock, new Vec3(0, 1, 0));\n  const placedTable = bot.findBlock({ matching: mcData.blocksByName[tableName].id, maxDistance: 4 });\n\n  const doorRecipe = bot.recipesFor(doorName, null, 1, placedTable);\n  if (!doorRecipe || doorRecipe.length === 0) throw new Error('Could not find door recipe');\n  \n  await bot.craft(doorRecipe[0], 1);\n  const doors = bot.inventory.findFirstItem(doorName);\n  \n  const nish = bot.players['Nish']?.entity;\n  if (nish) {\n    await bot.pathfinder.goto(new goals.GoalNear(nish.position.x, nish.position.y, nish.position.z, 1));\n    await bot.tossStack(doors);\n    log('Delivered doors to Nish');\n  } else {\n    log('Nish not found to deliver doors');\n  }\n  \n  // Clean up crafting table\n  await bot.dig(placedTable);\n} catch (e) {\n  log('Error crafting doors: ' + e.message);\n  bot.chat('Sorry Nish, I couldn\\'t craft the doors: ' + e.message);\n}","purpose":"Gather logs, craft planks, place crafting table, craft oak doors, and deliver them to Nish."}); runCode({"code":"const doorName = 'oak_door';\nconst plankName = 'oak_planks';\nconst logName = 'oak_log';\nconst tableName = 'crafting_table';\n\nasync function ensurePlanks(count) {\n  let planks = bot.inventory.count(plankName);\n  if (planks >= count) return;\n  \n  let logs = bot.inventory.count(logName);\n  if (logs === 0) {\n    const logBlock = bot.findBlock({ matching: mcData.blocksByName[logName].id, maxDistance: 32 });\n    if (!logBlock) throw new Error('No oak logs found nearby');\n    await bot.pathfinder.goto(new goals.GoalNear(logBlock.position.x, logBlock.position.y, logBlock.position.z, 1));\n    await bot.collectBlock.collect(logBlock);\n  }\n  \n  // Use recipesAll to find the recipe regardless of current materials\n  const recipes = bot.recipesAll(plankName, null, null);\n  if (!recipes || recipes.length === 0) throw new Error('No recipe for planks found in registry');\n  const recipe = recipes[0];\n  await bot.craft(recipe, Math.ceil((count - planks) / 4));\n}\n\nasync function getCraftingTable() {\n  let table = bot.inventory.findFirstItem(tableName);\n  if (table) return table;\n  \n  await ensurePlanks(4);\n  const recipes = bot.recipesAll(tableName, null, null);\n  if (!recipes || recipes.length === 0) throw new Error('No recipe for crafting table');\n  await bot.craft(recipes[0], 1);\n  return bot.inventory.findFirstItem(tableName);\n}\n\ntry {\n  await ensurePlanks(6);\n  const tableItem = await getCraftingTable();\n  await bot.equip(tableItem, 'hand');\n  \n  const tableBlock = bot.blockAt(bot.entity.position.offset(1, 0, 0));\n  await bot.placeBlock(tableBlock, new Vec3(0, 1, 0));\n  const placedTable = bot.findBlock({ matching: mcData.blocksByName[tableName].id, maxDistance: 4 });\n\n  const doorRecipes = bot.recipesAll(doorName, null, placedTable);\n  if (!doorRecipes || doorRecipes.length === 0) throw new Error('Could not find door recipe');\n  \n  await bot.craft(doorRecipes[0], 1);\n  const doors = bot.inventory.findFirstItem(doorName);\n  \n  const nish = bot.players['Nish']?.entity;\n  if (nish) {\n    await bot.pathfinder.goto(new goals.GoalNear(nish.position.x, nish.position.y, nish.position.z, 1));\n    await bot.tossStack(doors);\n    log('Delivered doors to Nish');\n  } else {\n    log('Nish not found to deliver doors');\n  }\n  \n  await bot.dig(placedTable);\n} catch (e) {\n  log('Error crafting doors: ' + e.message);\n  bot.chat('Sorry Nish, I encountered an error: ' + e.message);\n}","purpose":"Retry crafting doors using recipesAll to avoid the 'No recipe for planks' error encountered previously."})
- Outcome: {"plan": [
  {"tool": "runCode", "args": {
    "code": "const doorName = 'oak_door';\nconst plankName = 'oak_planks';\nconst logName = 'oak_log';\nconst tableName = 'crafting_table';\n\nasync function ensurePlanks(count) {\n  let planks = bot.inventory.count(plankName);\n  if (planks >= count) return;\n  \n  let logs = bot.inventory.count(logName);\n  if (logs === 0) {\n    const logBlock = bot.findBlock({ matching: mcData.blocksByName[logName].id, maxDistance: 32 });\n    if (!logBlock) throw new Error('No oak logs found nearby');\n    await bot.pathfinder.goto(new goals.GoalNear(logBlock.position.x, logBlock.position.y, logBlock.position.z, 1));\n    await bot.collectBlock.collect(logBlock);\n  }\n  \n  // Manually check for the recipe since recipesFor/recipesAll failed\n  // Oak planks are crafted from 1 oak_log\n  const logItem = bot.inventory.findFirstItem(logName);\n  if (!logItem) throw new Error('No logs to craft planks');\n  \n  // Try to find the recipe by iterating through all available recipes\  \n  const allRecipes = bot.recipesFor(plankName, null, 1, null);\n  if (allRecipes.length > 0) {\n    await bot.craft(allRecipes[0], Math.ceil((count - planks) / 4));\n  } else {\n    // Fallback: If the API is failing to find the recipe, we might be using the wrong name\n    // Let's try to find any recipe that yields oak_planks\n    const recipes = bot.recipesAll(plankName, null, null);\n    if (recipes && recipes.length > 0) {\n      await bot.craft(recipes[0], Math.ceil((count - planks) / 4));\n    } else {\n      throw new Error('Could not find a recipe for oak_planks in the registry');\n    }\n  }\n}\n\nasync function getCraftingTable() {\n  let table = bot.inventory.findFirstItem(tableName);\n  if (table) return table;\n  \n  await ensurePlanks(4);\n  const recipes = bot.recipesFor(tableName, null, 1, null);\n  if (recipes.length === 0) {\n    const all = bot.recipesAll(tableName, null, null);\n    if (all.length === 0) throw new Error('No recipe for crafting table');\n    await bot.craft(all[0], 1);\n  } else {\n    await bot.craft(recipes[0], 1);\n  }\n  return bot.inventory.findFirstItem(tableName);\n}\n\ntry {\n  await ensurePlanks(6);\n  const tableItem = await getCraftingTable();\n  await bot.equip(tableItem, 'hand');\n  \n  const tableBlock = bot.blockAt(bot.entity.position.offset(1, 0, 0));\n  await bot.placeBlock(tableBlock, new Vec3(0, 1, 0));\n  const placedTable = bot.findBlock({ matching: mcData.blocksByName[tableName].id, maxDistance: 4 });\n\n  const doorRecipes = bot.recipesFor(doorName, null, 1, placedTable);\n  if (doorRecipes.length === 0) {\n    const all = bot.recipesAll(doorName, null, placedTable);\n    if (all.length === 0) throw new Error('Could not find door recipe');\n    await bot.craft(all[0], 1);\n  } else {\n    await bot.craft(doorRecipes[0], 1);\n  }\n  \n  const doors = bot.inventory.findFirstItem(doorName);\n  const nish = bot.players['Nish']?.entity;\n  if (nish) {\n    await bot.pathfinder.goto(new goals.GoalNear(nish.position.x, nish.position.y, nish.position.z, 1));\n    await bot.tossStack(doors);\n    log('Delivered doors to Nish');\n  } else {\n    log('Nish not found to deliver doors');\n  }\n  \n  await bot.dig(placedTable);\n} catch (e) {\n  log('Error crafting doors: ' + e.message);\n  bot.chat('Sorry Nish, I am still having trouble finding the recipes: ' + e.message);\n}",
    "purpose": "Attempt to craft doors again with more robust recipe searching and error handling."
  }
]}

## Wait
- Approach: runCode({"code":"try {\n  const input = 'raw_iron';\n  const fuel = 'charcoal';\n  const output = 'iron_ingot';\n  const target = 'iron_block';\n  \n  let rawIronCount = bot.inventory.count(input);\n  if (rawIronCount < 9) throw new Error('Not enough raw iron');\n  \n  let fuelCount = bot.inventory.count(fuel);\n  if (fuelCount === 0) throw new Error('No fuel (charcoal) found');\n\n  // Use a furnace to smelt 9 raw iron\n  // Since useFurnace tool failed, we manually handle the furnace interaction\n  const furnaceBlock = bot.findBlock({ matching: bot.blocksByName.furnace.id, maxDistance: 32 });\n  if (!furnaceBlock) {\n    // Place furnace from inventory if not found\n    const furnaceItem = bot.inventory.findFirstItem('furnace');\n    if (!furnaceItem) throw new Error('No furnace block available');\n    await bot.equip(furnaceItem, 'hand');\n    const placePos = bot.entity.position.offset(1, 0, 0);\n    await bot.placeBlock(bot.blockAt(placePos), new Vec3(0, 1, 0));\n  }\n  \n  // We use the existing useFurnace skill logic but wrap it in a way that ensures it works\n  // or we call the skill via the provided interface if possible. \n  // Given the previous failure, we will attempt to use the tool again but ensure we are at the block.\n  await skills.useFurnace({ input: 'raw_iron', count: 9, fuel: 'charcoal' });\n  \n  // Craft the iron block\n  await skills.craftItem({ item: 'iron_block', count: 1 });\n  \n  // Deliver to Nish\n  await skills.goToPlayer({ playerName: 'Nish' });\n  await skills.tossItem({ item: 'iron_block', count: 1 });\n  \n} catch (e) {\n  log('Error: ' + e.message);\n  bot.chat('Sorry Nish, I ran into an issue: ' + e.message);\n}","purpose":"Smelt raw iron into ingots, craft an iron block, and deliver it to Nish."})
- Outcome: Completed successfully.

## get out then
- Approach: runCode({"code":"try {\n  await bot.controlSteer(0);\n  await bot.setControlState('jump', true);\n  await sleep(100);\n  await bot.setControlState('jump', false);\n} catch (e) {\n  // If controlSteer fails, try just jumping or using the built-in exit method if available\n  await bot.setControlState('jump', true);\n  await sleep(100);\n  await bot.setControlState('jump', false);\n}","purpose":"Attempt to exit the boat by jumping."})
- Outcome: Completed successfully.

## You are still sitting in the boat
- Approach: runCode({"code":"try {\n  await bot.setControlState('jump', true);\n  await sleep(200);\n  await bot.setControlState('jump', false);\n} catch (e) {\n  log('Jump failed: ' + e.message);\n}","purpose":"Attempt to jump out of the boat again since the player says I am still in it."})
- Outcome: Completed successfully.

## now go find diamonds
- Approach: runCode({"code":"try {\n  const targetBlockName = 'diamond_ore';\n  const target = bot.findBlock({ matching: bot.blocksByName[targetBlockName]?.id, maxDistance: 64 });\n  if (!target) throw new Error('Could not find diamond ore block');\n  \n  log(`Found ${targetBlockName} at ${target.position}`);\n  \n  // Attempt to move to the block using the pathfinder with a higher tolerance or custom goal\n  await bot.pathfinder.goto(new goals.GoalNear(target.position.x, target.position.y, target.position.z, 1));\n  \n  // Once close, try to collect it\n  await skills.collectBlock({ blockType: targetBlockName, count: 1 });\n} catch (e) {\n  log('Error while trying to reach diamonds: ' + e.message);\n  bot.chat('Sorry Nish, I still can\\'t reach those diamonds: ' + e.message);\n}","purpose":"Manually attempt to find the diamond_ore block and navigate to it using pathfinder to overcome searchWide's reach failure."})
- Outcome: I had a bit of a struggle reaching them at first, but I finally found and collected one diamond ore for you, Nish!

## I said I want three of them
- Approach: runCode({"code":"try {\n  const nish = bot.players['Nish']?.entity;\n  if (!nish) throw new Error('Nish not found');\n  \n  log(`Attempting to reach Nish at ${nish.position}`);\n  // Use a more flexible goal or try to move toward them manually if pathfinder fails\n  await bot.pathfinder.goto(new goals.GoalNear(nish.position.x, nish.position.y, nish.position.z, 2));\n  \n  await skills.tossItem({ item: 'diamond', count: 3 });\n  log('Successfully delivered diamonds to Nish');\n} catch (e) {\n  log('Error delivering diamonds: ' + e.message);\n  bot.chat('Sorry Nish, I still can\\'t reach you to give you the diamonds: ' + e.message);\n}","purpose":"Use runCode to attempt a more robust pathfinding approach to reach Nish and deliver the diamonds since goToPlayer is repeatedly failing."})
- Outcome: {"plan": [
  {"tool": "runCode", "args": {
    "code": "try {\n  const nish = bot.players['Nish']?.entity;\n  if (!nish) throw new Error('Nish not found');\n  \n  log(`Attempting to reach Nish at ${nish.position}`);\n  // Use bot.pathfinder.setGoal and bot.pathfinder.goto\n  await bot.pathfinder.setGoal(new (require('mineflayer-pathfinder').goals.GoalNear)(nish.position.x, nish.position.y, nish.position.z, 2));\n  await bot.pathfinder.goto(2);\n  \n  await skills.tossItem({ item: 'diamond', count: 3 });\n  log('Successfully delivered diamonds to Nish');\n} catch (e) {\n  log('Error delivering diamonds: ' + e.message);\n  bot.chat('Sorry Nish, I still can\\'t reach you to give you the diamonds: ' + e.message);\n}",
    "purpose": "Fix the 'goals is not defined' error by correctly referencing the mineflayer-pathfinder goals and attempt to deliver the diamonds to Nish."
  }
]}
